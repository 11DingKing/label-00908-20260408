<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Redis 缓冲模式开关
    |--------------------------------------------------------------------------
    | 开启后，使用量记录将先写入 Redis 缓冲区，再由定时任务批量写入数据库
    | 适用于高并发场景（如 API 调用计费），可显著提升写入吞吐量
    */
    'buffer_enabled' => env('USAGE_BUFFER_ENABLED', false),

    /*
    |--------------------------------------------------------------------------
    | 批量写入配置
    |--------------------------------------------------------------------------
    */
    'batch_size' => (int) env('USAGE_BUFFER_BATCH_SIZE', 1000),

    /*
    |--------------------------------------------------------------------------
    | 缓冲区刷新间隔（秒）
    |--------------------------------------------------------------------------
    | 定时任务执行间隔，建议设置为 60 秒
    */
    'flush_interval' => (int) env('USAGE_BUFFER_FLUSH_INTERVAL', 60),
];
